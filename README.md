# Multiplication Table Generator (Python)

This project generates multiplication tables from 2 to 20
and saves them into a text file using Python.

## Features
- Uses functions
- Uses loops
- Uses file handling
- Beginner friendly project

## Output
The output is saved in `table.txt`
